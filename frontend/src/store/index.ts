import { createStore } from 'vuex';

export default createStore({
  state: {
  },
  getters: {
    isAdmin: (state) => {
      return true;
    }
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
